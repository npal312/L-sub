package com.example.num2let;

public class NegativeNumberHandler {
	public String handleNegative(int number) {
		//– Applies the negative number representation.
		return "";
	}
	public void setNegativePrefix(String prefix) {
		//– Allows setting a custom prefix for negative numbers (checks if in the WordMap though).
	}
}
